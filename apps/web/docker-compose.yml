services:
  postgres-test:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: testlens
      POSTGRES_USER: testlens
      POSTGRES_PASSWORD: testlens
    tmpfs:
      - /var/lib/postgresql/data
